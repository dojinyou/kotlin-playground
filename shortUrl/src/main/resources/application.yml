spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13301/short_url
    username: root
    password: mysql
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        show_sql: true

server:
  port: 18081
