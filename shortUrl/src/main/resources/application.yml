spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:13301/short_url
    username: root
    password: mysql
  data:
    redis:
      host: 127.0.0.1
      port: 16371
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true

server:
  port: 18081
